// Right panel — case study viewer
// Fetches and renders real PROJECT_NARRATIVE.md files
// from framework/ via the GitHub raw content API.
// Status: placeholder — fetch + render in a later task

export default function CaseStudyPanel() {
  return (
    <section className="panel" aria-label="Case Studies">
      <p className="panel-label">Case Studies</p>
      <p className="panel-placeholder">
        Real project narratives from framework/ — coming later
      </p>
    </section>
  )
}
