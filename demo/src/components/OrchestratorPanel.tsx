// Center panel — live Claude API orchestrator
// User enters a project idea; framework decomposes it into
// phases, risks, agent assignments, and complexity tiers.
// Status: placeholder — Claude API wiring in next task

export default function OrchestratorPanel() {
  return (
    <section className="panel" aria-label="Orchestrator">
      <p className="panel-label">Try It Live</p>
      <p className="panel-placeholder">
        Claude API orchestrator — coming next task
      </p>
    </section>
  )
}
